# ===================================================================
# 🗄️ CLOUD DATABASE CONFIGURATION (RAILWAY PROTOCOL FIXED)
# ===================================================================
spring.datasource.url=jdbc:mysql://junction.proxy.rlwy.net:30689/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

spring.datasource.username=root
spring.datasource.password=UUVXMAWzKsHxkWirgfBhscyAhlOxxrBO
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# 🚀 HIBERNATE & JPA SETTINGS (DIALECT ENFORCED)
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# 🔑 FIX: Hibernate ko automatic dialect dhoondhne ke liye mana kiya, direct assign kar diya:
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Metadata retrieval fail hone par application ko crash hone se rokne ke liye:
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# ===================================================================
# ⚡ HIKARI CONNECTION POOL SETTINGS
# ===================================================================
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
