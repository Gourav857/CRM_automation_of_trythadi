# ===================================================================
# 🚀 AIVEN CLOUD DATABASE CONFIGURATION
# ===================================================================
# ⚠️ dhyan dein: yahan jdbc:mysql:// aur aakhir me port aur database ka naam hona zaroori hai!
server.port=${PORT:8080}
spring.datasource.url=jdbc:mysql://crm-mysql-live-ydasratkumar-fd37.e.aivencloud.com:25433/defaultdb
spring.datasource.username=avnadmin
spring.datasource.password=AVNS_oDG0MF3_Dmw3yE72p0K
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===================================================================
# 📧 Enterprise Java Mail Sender SSL Pipeline
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}

# Secure SSL configurations for Render bypass
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000


# 🚀 Enterprise Cloud Redis Fully Secured SSL Pipeline
spring.data.redis.host=${SPRING_DATA_REDIS_HOST}
spring.data.redis.port=${SPRING_DATA_REDIS_PORT:6379}
spring.data.redis.password=${SPRING_DATA_REDIS_PASSWORD}

# Cloud Redis security rules compliance (Crucial Step)
spring.data.redis.ssl.enabled=true
