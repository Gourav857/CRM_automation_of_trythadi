# ===================================================================
# 🗄️ CLOUD DATABASE CONFIGURATION (RAILWAY MYSQL PUBLIC - FIXED)
# ===================================================================
# Added SSL and connection parameters to avoid handshake failures on cloud

spring.datasource.url=jdbc:mysql://junction.proxy.rlwy.net:30689/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=UUVXMAWzKsHxkWirgfBhscyAhlOxxrBO
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# 🚀 HIBERNATE & JPA SETTINGS
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Removed explicit MySQLDialect to let Spring Boot auto-detect (fixes HHH90000025 warning)

# Server Port Control for Cloud environments
server.port=${PORT:8080}
