# ===================================================================
# 🗄️ CLOUD DATABASE CONFIGURATION (RAILWAY MYSQL - TUNED)
# ===================================================================
spring.datasource.url=jdbc:mysql://junction.proxy.rlwy.net:30689/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&connectTimeout=5000&socketTimeout=30000
spring.datasource.username=root
spring.datasource.password= UUVXMAWzKsHxkWirgfBhscyAhlOxxrBO
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# ⚡ HIKARI CONNECTION POOL OPTIMIZATION (FAST BOOT)
# ===================================================================
# Cloud ki latency kam karne aur fast injection ke liye settings
spring.datasource.hikari.connection-timeout=15000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000

# ===================================================================
# 🚀 HIBERNATE & JPA SETTINGS
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20

# Force Render Port recognition early
server.port=10000
